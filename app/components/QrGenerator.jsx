const QrGenerator = ({ name, peludo }) => {
  // const [qrState, setQrState] = useState('')

  // useEffect(() => {
  //   setQrState('url', name, peludo)
  // }, [name, peludo])

  return (
    <div>
      https://programaJustLikeHome.com/
      {name}
      {peludo}
    </div>
  )
}

export default QrGenerator
